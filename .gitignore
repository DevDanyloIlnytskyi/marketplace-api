node_modules/

# Environment (never commit secrets)
.env
.env.local
.env.*.local
.env.postgres.staging
!.env.example
!.env.postgres.staging.example

# Tenant registry and API keys (commit examples only)
config/api-keys.json
config/clients.json
config/clients.poc.postgres.json

# Legacy upload dir — keep placeholder only
images/*
!images/.gitkeep

# Tenant runtime storage — placeholder only in git
storage/*
!storage/.gitkeep
config/clients/*.json
!config/clients/client.template.json

# Logs and debug dumps
*.log
logs/
*.dump
migration-report*.json
audit-*.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
