# Copy to .env and fill in local/production values. Do not commit .env.
# Variable names match api/sequelize.js (not DB_HOST-style aliases).

port=5000
host=127.0.0.1
db_port=3306
user=your_mysql_user
password=your_mysql_password
database=your_database_name
JWTKEY=your_jwt_secret_min_32_chars
# Legacy alias (optional): jwtkey=your_jwt_secret_min_32_chars

# Tenant registry — required in production (absolute path on server)
# Development (from api/): ../config/clients.json
# Production: /opt/marketplace/config/clients.json
TENANT_REGISTRY_PATH=../config/clients.json
TENANT_DEV_DOMAIN=avtoleg.local

# Integration smoke scripts — optional (default: first active tenant in registry)
# SMOKE_TENANT_ID=
# SMOKE_TENANT_DOMAIN=

# Tenant media root. Required when NODE_ENV=production.
# Production canonical path: /opt/marketplace/storage
# Development: omit to use api/storage/ (repo-relative fallback)
# MARKETPLACE_STORAGE_ROOT=/opt/marketplace/storage
